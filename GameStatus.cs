#nullable disable
using System;
using SplashKitSDK;

namespace testCustom
{
    /// <summary>
    /// This is an enumeration which shows the game state.
    /// </summary>
    public enum GameStatus{
        Ready,

        Start,

        Win,

        Lose,

        Pause,

        End
    }
}